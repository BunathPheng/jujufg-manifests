---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jujufg                  # Ingress name
  labels:
    app: jujufg
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "jujufg.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jujufg               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
